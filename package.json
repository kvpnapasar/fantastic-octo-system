{
  "name": "sahyadri-healthcare-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "build:cloudflare": "npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
